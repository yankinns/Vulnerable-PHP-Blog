<?php require_once 'config.php'; ?>
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Уязвимый Блог - Главная</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="index.php">Уязвимый Блог</a>
            <div class="navbar-nav">
                <a class="nav-link" href="index.php">Главная</a>
                <a class="nav-link" href="search.php">Поиск</a>
                <?php if(is_admin()): ?>
                    <a class="nav-link" href="dashboard.php">Панель управления</a>
                    <a class="nav-link" href="logout.php">Выйти</a>
                <?php else: ?>
                    <a class="nav-link" href="login.php">Вход</a>
                <?php endif; ?>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <h1>Последние посты</h1>
        
        <?php
       
        $order = isset($_GET['order']) ? $_GET['order'] : 'id';
        
        
        $sql = "SELECT p.*, u.username FROM posts p 
                LEFT JOIN users u ON p.author_id = u.id 
                ORDER BY " . $order . " DESC";
        
        $result = mysqli_query($conn, $sql);
        
        while($row = mysqli_fetch_assoc($result)):
        ?>
            <div class="card mb-3">
                <div class="card-body">
                    <h5 class="card-title">
                        <a href="view_post.php?id=<?php echo $row['id']; ?>">
                            <?php echo $row['title']; ?>
                        </a>
                    </h5>
                    
                  
                    <p class="card-text"><?php echo $row['content']; ?></p>
                    
                    <small class="text-muted">
                        Автор: <?php echo htmlspecialchars($row['username']); ?> | 
                        Дата: <?php echo $row['created_at']; ?>
                    </small>
                </div>
            </div>
        <?php endwhile; ?>
        
        <!-- Демонстрация работы с GET-параметром -->
        <div class="mt-3">
            <p>Сортировка: 
                <a href="?order=id">По ID</a> | 
                <a href="?order=created_at">По дате</a>
            </p>
        </div>
    </div>
</body>
</html>
